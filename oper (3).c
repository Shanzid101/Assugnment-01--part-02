#include <stdio.h>

int main() {
    float num1, num2, sum;
    printf("Enter two numbers: ");
    scanf("%f %f", &num1, &b);
    
    sum = (3.31 * num1 * num1 + 2.01 * num2 * num2 * num2) / (7.16 * num2 * num2 + 2.01 * num1 * num1 * num1);
    printf("X = %0.5f\n", sum);
    
    return 0;
}
